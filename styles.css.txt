:root {
  --bg: #f7f2ec;
  --surface: #ffffff;
  --surface-soft: #fff8f1;
  --ink: #2d241f;
  --muted: #71645d;
  --brand: #8f3b12;
  --brand-dark: #6f2d0e;
  --brand-soft: #f5dfcf;
  --line: #e5d8cd;
  --success: #1f7a4d;
  --success-soft: #e4f5ec;
  --warning: #946200;
  --warning-soft: #fff2cc;
  --danger: #b42318;
  --danger-soft: #fde9e7;
  --shadow: 0 18px 50px rgba(72, 42, 26, 0.10);
  --radius-lg: 24px;
  --radius-md: 16px;
  --radius-sm: 12px;
  font-family: "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif;
}

* { box-sizing: border-box; }

html { min-height: 100%; background: var(--bg); }

body {
  min-height: 100vh;
  margin: 0;
  color: var(--ink);
  background:
    radial-gradient(circle at top right, rgba(143, 59, 18, 0.10), transparent 28rem),
    var(--bg);
}

button, input { font: inherit; }
button { touch-action: manipulation; }

.app-shell {
  width: min(100%, 760px);
  min-height: 100vh;
  margin: 0 auto;
  padding: env(safe-area-inset-top) 16px env(safe-area-inset-bottom);
}

.app-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  padding: 28px 4px 20px;
}

.eyebrow, .step-label {
  margin: 0 0 6px;
  color: var(--brand);
  font-size: 0.82rem;
  font-weight: 800;
  letter-spacing: 0.08em;
}

h1, h2, h3, p { margin-top: 0; }
h1 { margin-bottom: 0; font-size: clamp(1.55rem, 6vw, 2.15rem); line-height: 1.2; }
h2 { margin-bottom: 8px; font-size: clamp(1.35rem, 5vw, 1.75rem); }
h3 { margin-bottom: 8px; font-size: 1.15rem; }
p { color: var(--muted); line-height: 1.7; }

.status-badge, .role-badge {
  display: inline-flex;
  align-items: center;
  min-height: 32px;
  padding: 6px 11px;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 800;
  white-space: nowrap;
}

.status-badge--checking { color: var(--warning); background: var(--warning-soft); }
.status-badge--online { color: var(--success); background: var(--success-soft); }
.status-badge--offline { color: var(--danger); background: var(--danger-soft); }
.role-badge { color: var(--brand-dark); background: var(--brand-soft); }

.app-main { padding-bottom: 28px; }

.card, .notice-card {
  border: 1px solid rgba(143, 59, 18, 0.10);
  border-radius: var(--radius-lg);
  background: var(--surface);
  box-shadow: var(--shadow);
}

.card { padding: clamp(20px, 5vw, 34px); }
.notice-card { margin-bottom: 16px; padding: 18px; }
.notice-card--danger { border-color: rgba(180, 35, 24, 0.25); background: var(--danger-soft); }
.notice-card p { margin-bottom: 0; }

.card-heading { margin-bottom: 24px; }
.card-heading p:last-child { margin-bottom: 0; }

.field-group { margin-bottom: 20px; }
label { display: block; margin-bottom: 8px; font-weight: 800; }

input {
  width: 100%;
  min-height: 52px;
  border: 1px solid var(--line);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
  color: var(--ink);
  background: #fff;
  outline: none;
  transition: border-color 0.15s ease, box-shadow 0.15s ease;
}

input:focus {
  border-color: var(--brand);
  box-shadow: 0 0 0 4px rgba(143, 59, 18, 0.12);
}

.field-hint { margin: 7px 2px 0; font-size: 0.82rem; }

.password-wrap { position: relative; }
.password-wrap input { padding-right: 78px; }
.icon-button {
  position: absolute;
  top: 50%;
  right: 8px;
  transform: translateY(-50%);
  min-height: 38px;
  border: 0;
  border-radius: 10px;
  padding: 0 12px;
  color: var(--brand);
  background: var(--brand-soft);
  font-weight: 800;
  cursor: pointer;
}

.primary-button, .secondary-button, .danger-button {
  min-height: 50px;
  border-radius: var(--radius-sm);
  padding: 12px 18px;
  font-weight: 800;
  cursor: pointer;
  transition: transform 0.12s ease, opacity 0.12s ease;
}

.primary-button:active, .secondary-button:active, .danger-button:active { transform: scale(0.985); }
button:disabled { cursor: not-allowed; opacity: 0.58; }

.primary-button {
  position: relative;
  width: 100%;
  border: 0;
  color: #fff;
  background: linear-gradient(135deg, var(--brand), var(--brand-dark));
  box-shadow: 0 12px 24px rgba(143, 59, 18, 0.22);
}

.secondary-button { border: 1px solid var(--line); color: var(--ink); background: var(--surface-soft); }
.danger-button { border: 1px solid rgba(180, 35, 24, 0.25); color: var(--danger); background: var(--danger-soft); }

.button-spinner {
  display: none;
  width: 18px;
  height: 18px;
  margin-left: 8px;
  border: 2px solid rgba(255,255,255,0.45);
  border-top-color: #fff;
  border-radius: 50%;
  vertical-align: middle;
  animation: spin 0.8s linear infinite;
}

.primary-button.is-loading .button-spinner { display: inline-block; }
@keyframes spin { to { transform: rotate(360deg); } }

.form-message {
  margin: 0 0 16px;
  border-radius: 12px;
  padding: 12px 14px;
  line-height: 1.55;
  font-size: 0.92rem;
}
.form-message--error { color: var(--danger); background: var(--danger-soft); }
.form-message--success { color: var(--success); background: var(--success-soft); }
.form-message--info { color: var(--warning); background: var(--warning-soft); }

.privacy-note {
  margin-top: 24px;
  border-top: 1px solid var(--line);
  padding-top: 20px;
}
.privacy-note p { margin: 5px 0 0; font-size: 0.87rem; }

.dashboard { display: grid; gap: 16px; }
.profile-topline { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }
.profile-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin: 22px 0 0;
}
.profile-grid > div { min-width: 0; border-radius: var(--radius-sm); padding: 13px; background: var(--surface-soft); }
dt { margin-bottom: 4px; color: var(--muted); font-size: 0.78rem; font-weight: 700; }
dd { margin: 0; overflow-wrap: anywhere; font-weight: 800; }

.action-grid { display: grid; gap: 10px; margin-top: 18px; }
.phase-card .form-message { margin: 16px 0 0; }

.app-footer {
  display: flex;
  justify-content: center;
  gap: 12px;
  padding: 4px 0 24px;
  color: var(--muted);
  font-size: 0.78rem;
}

code { overflow-wrap: anywhere; }
[hidden] { display: none !important; }

@media (min-width: 640px) {
  .app-shell { padding-left: 24px; padding-right: 24px; }
  .action-grid { grid-template-columns: repeat(3, 1fr); }
}

@media (max-width: 420px) {
  .app-header { align-items: stretch; flex-direction: column; }
  .status-badge { align-self: flex-start; }
  .profile-grid { grid-template-columns: 1fr; }
  .profile-topline { flex-direction: column; }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { scroll-behavior: auto !important; animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}
